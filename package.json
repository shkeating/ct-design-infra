{
  "name": "ct-design-infra-root",
  "private": true,
  "description": "Root of the Civic-Theme infrastructure monorepo",
  "scripts": {
    "build:tokens": "pnpm --filter @ct-infra/tokens run build",
    "build:core": "pnpm --filter @ct-infra/core run build",
    "dev:core": "pnpm --filter @ct-infra/core run dev",
    "verify:component": "node scripts/verify-component.mjs"
  },
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.11.0",
    "@playwright/test": "^1.59.1",
    "vite-plugin-cem": "^0.8.5"
  }
}
