<ct-tooltip
  theme="{{theme}}"
  position="{{position}}"
  icon="{{icon}}"
  icon-size="{{iconSize}}"
  label="{{label}}"
  content="{{content}}"
  {{#if open}}open{{/if}}
  {{#if modifierClass}}modifier-class="{{modifierClass}}"{{/if}}
></ct-tooltip>
