<ct-content-link
  theme="{{theme}}"
  text="{{text}}"
  {{#if url}}url="{{url}}"{{/if}}
  {{#if linkTitle}}title="{{linkTitle}}"{{/if}}
  {{#if newWindow}}new-window{{/if}}
  {{#if external}}external{{/if}}
  {{#if modifierClass}}modifier-class="{{modifierClass}}"{{/if}}
></ct-content-link>
