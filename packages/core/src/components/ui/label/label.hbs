<ct-label
  theme="{{theme}}"
  tag="{{tag}}"
  content="{{content}}"
  size="{{size}}"
  {{#if required}}required{{/if}}
  {{#if requiredText}}required-text="{{requiredText}}"{{/if}}
  {{#if for}}for="{{for}}"{{/if}}
  {{#if modifierClass}}modifier-class="{{modifierClass}}"{{/if}}
></ct-label>
