<ct-image
  theme="{{theme}}"
  url="{{url}}"
  alt="{{alt}}"
  {{#if width}}width="{{width}}"{{/if}}
  {{#if height}}height="{{height}}"{{/if}}
  {{#if modifierClass}}modifier-class="{{modifierClass}}"{{/if}}
></ct-image>
