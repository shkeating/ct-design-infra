<ct-button 
  theme="{{theme}}" 
  kind="{{kind}}" 
  variant="{{variant}}" 
  size="{{size}}" 
  label="{{label}}" 
  icon-placement="{{iconPlacement}}"
  {{#if url}}url="{{url}}"{{/if}}
  {{#if icon}}icon="{{icon}}"{{/if}}
  {{#if disabled}}disabled{{/if}}
  {{#if newWindow}}new-window{{/if}}
  {{#if external}}external{{/if}}
  {{#if dismissable}}dismissable{{/if}}
  {{#if modifierClass}}modifier-class="{{modifierClass}}"{{/if}}
></ct-button>
