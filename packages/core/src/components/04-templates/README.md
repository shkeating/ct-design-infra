# Templates

Reserved for CivicTheme template-tier components (page, event — see
`packages/sdc/components/04-templates` in `civictheme/uikit`). None have been ported
yet; this file exists so the tier directory is tracked by git ahead of the first
component landing here.
