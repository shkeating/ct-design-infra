<ct-chip
  theme="{{theme}}"
  kind="{{kind}}"
  size="{{size}}"
  content="{{content}}"
  {{#if url}}url="{{url}}"{{/if}}
  {{#if label}}label="{{label}}"{{/if}}
  {{#if selected}}selected{{/if}}
  {{#if disabled}}disabled{{/if}}
></ct-chip>
