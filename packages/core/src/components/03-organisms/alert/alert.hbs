<ct-alert
  theme="{{theme}}"
  type="{{type}}"
  heading="{{heading}}"
  description="{{description}}"
  {{#if identifier}}identifier="{{identifier}}"{{/if}}
  {{#if noDismiss}}no-dismiss{{/if}}
  {{#if modifierClass}}modifier-class="{{modifierClass}}"{{/if}}
></ct-alert>
